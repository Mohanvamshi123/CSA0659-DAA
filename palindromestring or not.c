#include<stdio.h>
#include<string.h>
int main()
{
char str1[50],srt2[50];
print("\n enter the string to be check the palindrom:")
gets(str1);
strcpy(str2,str1);
strrev(str2);
if(strcpy(str1,str2)==0)
printf("string is palindrome:")
else
printf("string is not palindrome:")
return 0;
}